# spring:
#   application:
#     name: product-management-api
#   main:
#     banner-mode: off
#   datasource:
#     url: jdbc:postgresql://localhost:5432/product_management
#     username: postgres
#     password: 1234
#     driver-class-name: org.postgresql.Driver
#   jpa:
#     hibernate:
#       ddl-auto: update
#     show-sql: true

server:
 port: 8080
spring:
 application:
   name: product-management-api
 main:
   banner-mode: console
 datasource:
   driver-class-name: org.postgresql.Driver
   url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/product_management}
   username: ${SPRING_DATASOURCE_USERNAME:postgres}
   password: ${SPRING_DATASOURCE_PASSWORD:1234}
 jpa:
   hibernate:
     ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
   show-sql: true
   properties:
     hibernate:
       dialect: org.hibernate.dialect.PostgreSQLDialect

springdoc:
 swagger-ui:
   path: /swagger-ui.html
   enabled: true
   operationsSorter: method
   servers:
     - url: http://localhost:8081
       description: Docker External Port
 api-docs:
   path: /v3/api-docs
   enabled: true
